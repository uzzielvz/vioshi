# HOY

A1 + F + D están en `main`. No hay paquete LISTO. No lances agentes a inventar trabajo.

## Siguiente código

Solo **E Connect**, cuando Uzziel marque las dos casillas de `packets/E-connect.md` y `STATUS.md` diga `L7 desbloqueado`.

## Cómo usar lo de hoy

| Qué | Dónde |
|---|---|
| Precio envío a domicilio | `/admin/settings` → “Envío a domicilio (MXN)”. Semilla $10 |
| Pickup | `/admin/pickup-points` (por fila, no el setting de domicilio) |
| Tienda Viogi | `/es/tienda/viogi` |
| “Vendido por Viogi” | ficha de producto |
| Solicitud de vendedor | `/es/vender` → fila `pending` en `store_applications` (Supabase). No se aprueba sola |
| Embed al publicar | crear/editar en `/admin/products`. Si Gemini falla: banner “Embedding pendiente” |
| Marcar entregado | `/admin/reservas` → “Por entregar” |

Connect **no** transfiere dinero al marcar Entregado. Eso es E.

## Git

Sigue valiendo `GIT.md`: rama + PR, nunca push a `main`.
