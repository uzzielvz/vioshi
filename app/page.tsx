import SplashScreen from "@/components/SplashScreen";

export default function Home() {
  // Página principal = Splash Screen artístico
  // Al dar click navega a:
  // - Mobile: /collections/all (VER TODO)
  // - Desktop: /collections/new (NUEVO DROP)
  return <SplashScreen />;
}
